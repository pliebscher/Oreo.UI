export interface nwsData {

    temperature: string[]
    pop: string[] // Prob. of Precip.
    weather: string[]
    iconLink: string[]
    text: string[]
    hazard: string[]
    hazardUrl: string[]
    
}